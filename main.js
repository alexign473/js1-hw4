let myNumber = +prompt('Enter the number: ')

function parse(myNumber) {
    as
}

let testNumber = 512

let parsedNumber = {
    ones: '2',
    twos: '1',
    threes: '5',

}

console.log(testNumber, parsedNumber)